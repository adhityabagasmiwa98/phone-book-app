package com.adhityabagasmiwa.phonebook.infrastructures.persistences.api

import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.flow.flow

/*
class AuthApiPersistence(private var authApi: AuthApi) : AuthPersistenceInterface {
    override suspend fun register(auth: HashMap<String, Any?>): Flow<RegisterResponseInterface> {
        return flow {
            emit(authApi.register(auth))
        }
    }
}*/
